const navToggle=document.querySelector(".nav-toggle");const nav=document.querySelector(".nav");if(navToggle){navToggle.addEventListener("click",()=>{nav.classList.toggle("open")})}
const yearEl=document.getElementById("year");if(yearEl){yearEl.textContent=String(new Date().getFullYear())}
const form=document.getElementById("contact-form");const statusEl=document.getElementById("form-status");if(form){form.addEventListener("submit",e=>{e.preventDefault();const name=document.getElementById("name");const email=document.getElementById("email");const message=document.getElementById("message");const emailOk=/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.value);if(!name.value.trim()){statusEl.textContent="Nama wajib diisi";statusEl.style.color="#ef4444";return}if(!emailOk){statusEl.textContent="Email tidak valid";statusEl.style.color="#ef4444";return}if(!message.value.trim()){statusEl.textContent="Pesan wajib diisi";statusEl.style.color="#ef4444";return}statusEl.textContent="Terima kasih, pesan Anda terkirim.";statusEl.style.color="#22c55e";form.reset()})}
